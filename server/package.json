{
  "name": "guestbook-api",
  "version": "1.0.0",
  "description": "Guestbook API with Cloudflare D1",
  "main": "src/server.js",
  "scripts": {
    "dev": "NODE_ENV=development node src/server.js",
    "deploy": "wrangler publish",
    "migrate": "wrangler d1 execute guestbook-db --file=migrations/0001_create_messages_table.sql"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "better-sqlite3": "^8.0.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@cloudflare/wrangler-d1": "^0.0.1"
  }
}
